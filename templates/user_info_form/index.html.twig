{% extends 'base.html.twig' %}

{% block title %}Hello UserInfoFormController!{% endblock %}

{% block body %}

<div>
    <h1>Hello!</h1>
    <div class="form-inline justify-content-center">

    {{ form_start(usersform) }}
    {{ form(usersform) }}
    {{ form_widget(usersform) }}
    {{ form_end(usersform) }}
    </div>


    <div class="form-inline justify-content-center">
        <div class="col-sm-2"></div>
         <div class="col-sm-2">
             <input type="submit" value="Submit" class="btn btn-success" />
        </div>
    </div>
</div>

{% endblock %}